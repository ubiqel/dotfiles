# TODO: get list of player's statuses via playerctl status -a each line will
# contain status "Playing" or "Paused". If any of the lines contains "Playing"
# (one of the players is playing), then return "true". Else return "false"
